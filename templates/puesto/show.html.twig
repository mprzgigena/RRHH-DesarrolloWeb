{% extends 'base.html.twig' %}

{% block title %}Detalle del Puesto{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Detalle del Puesto</h1>

    <div class="card">
        <div class="card-body">
            <h5 class="card-title">Información del puesto</h5>
            <ul class="list-group">
                <li class="list-group-item"><strong>ID:</strong> {{ puesto.id }}</li>
                <li class="list-group-item"><strong>Nombre:</strong> {{ puesto.nombre }}</li>
            </ul>

            <a href="{{ path('app_puesto_edit', {'id': puesto.id}) }}" class="btn btn-primary mt-3">Editar</a>
            <a href="{{ path('puesto_index') }}" class="btn btn-secondary mt-3">Volver al listado</a>
        </div>
    </div>
</div>
{% endblock %}
