{% extends 'base.html.twig' %}

{% block title %}Puesto nuevo{% endblock %}

{% block body %}
    <h1>Crear nuevo puesto</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('Actualizar') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('puesto_index') }}">back to list</a>
{% endblock %}
