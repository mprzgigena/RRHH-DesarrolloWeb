{# templates/_card_estudiante.html.twig #}
{% import '_ui_macros.html.twig' as ui %}

<div class="card" style="width: 18rem; margin-bottom: 20px; border: 1px solid #ccc; border-radius: 8px; padding: 15px;">
    
    <h3>{{ estudiante.nombre }}</h3>
    <p>Nota: {{ estudiante.nota }}</p>
    
    {# Usamos la macro para crear un botón #}
    {{ ui.boton('boton macro', 'btn btn-info') }}
</div>